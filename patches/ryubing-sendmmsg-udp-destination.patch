diff --git a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/IClient.cs b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/IClient.cs
index ef3b68b27..c2cb105ae 100644
--- a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/IClient.cs
+++ b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/IClient.cs
@@ -553,11 +553,11 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd
 
                     receiveRegion.Dispose();
 
-                    if (sockAddrOutSize != 0 && sockAddrOutSize >= (ulong)Unsafe.SizeOf<BsdSockAddr>())
+                    if (sockAddrOutSize >= (ulong)Unsafe.SizeOf<BsdSockAddr>())
                     {
                         context.Memory.Write(sockAddrOutPosition, BsdSockAddr.FromIPEndPoint(endPoint));
                     }
-                    else
+                    else if (sockAddrOutSize != 0)
                     {
                         errno = LinuxError.ENOMEM;
                     }
@@ -850,6 +850,21 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd
                         context.Memory.Write(bufferPosition, 0);
                         break;
 
+                    case BsdIoctl.FionRead:
+                        errno = LinuxError.SUCCESS;
+
+                        (bufferPosition, bufferSize) = context.Request.GetBufferType0x22();
+
+                        if (bufferSize >= sizeof(int))
+                        {
+                            context.Memory.Write(bufferPosition, socket.GetReadableByteCount());
+                        }
+                        else
+                        {
+                            errno = LinuxError.ENOMEM;
+                        }
+                        break;
+
                     default:
                         errno = LinuxError.EOPNOTSUPP;
 
diff --git a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/ISocket.cs b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/ISocket.cs
index cf32daf3b..87d4a5562 100644
--- a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/ISocket.cs
+++ b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/ISocket.cs
@@ -16,6 +16,8 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd
 
         ProtocolType ProtocolType { get; }
 
+        int GetReadableByteCount();
+
         nint Handle { get; }
 
         LinuxError Receive(out int receiveSize, Span<byte> buffer, BsdSocketFlags flags);
diff --git a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Impl/ManagedSocket.cs b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Impl/ManagedSocket.cs
index 0624fcf91..95fce775e 100644
--- a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Impl/ManagedSocket.cs
+++ b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Impl/ManagedSocket.cs
@@ -23,6 +23,17 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Impl
 
         public nint Handle => nint.Zero;
 
+        public int GetReadableByteCount()
+        {
+            int available = Socket.Available;
+            if (SocketType != SocketType.Dgram || available == 0)
+            {
+                return available;
+            }
+
+            return Socket.Receive(new byte[available], SocketFlags.Peek);
+        }
+
         public IPEndPoint RemoteEndPoint => Socket.RemoteEndPoint as IPEndPoint;
 
         public IPEndPoint LocalEndPoint => Socket.LocalEndPoint as IPEndPoint;
@@ -118,6 +129,12 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Impl
             Logger.Info?.PrintMsg(LogClass.ServiceBsd, $"Socket binding to: {ProtocolType}/{localEndPoint.Port}");
             try
             {
+                if (Socket.AddressFamily == AddressFamily.InterNetworkV6 &&
+                    localEndPoint.AddressFamily == AddressFamily.InterNetwork)
+                {
+                    localEndPoint = new IPEndPoint(IPAddress.IPv6Any, localEndPoint.Port);
+                }
+
                 Socket.Bind(localEndPoint);
 
                 return LinuxError.SUCCESS;
@@ -254,7 +271,16 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Impl
                     _hasEmittedBlockingWarning = true;
                 }
 
-                receiveSize = Socket.Receive(buffer, ConvertBsdSocketFlags(flags));
+                if (SocketType == SocketType.Dgram &&
+                    flags.HasFlag(BsdSocketFlags.Peek) &&
+                    flags.HasFlag(BsdSocketFlags.Trunc))
+                {
+                    receiveSize = GetReadableByteCount();
+                }
+                else
+                {
+                    receiveSize = Socket.Receive(buffer, ConvertBsdSocketFlags(flags));
+                }
 
                 result = LinuxError.SUCCESS;
             }
@@ -309,7 +335,18 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Impl
                     return LinuxError.EOPNOTSUPP;
                 }
 
-                receiveSize = Socket.ReceiveFrom(buffer[..size], ConvertBsdSocketFlags(flags), ref temp);
+                if (SocketType == SocketType.Dgram &&
+                    flags.HasFlag(BsdSocketFlags.Peek) &&
+                    flags.HasFlag(BsdSocketFlags.Trunc))
+                {
+                    int available = Socket.Available;
+                    receiveSize = Socket.ReceiveFrom(new byte[Math.Max(available, 1)],
+                        SocketFlags.Peek, ref temp);
+                }
+                else
+                {
+                    receiveSize = Socket.ReceiveFrom(buffer[..size], ConvertBsdSocketFlags(flags), ref temp);
+                }
 
                 remoteEndPoint = (IPEndPoint)temp;
                 result = LinuxError.SUCCESS;
@@ -359,6 +396,12 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Impl
         {
             try
             {
+                if (Socket.AddressFamily == AddressFamily.InterNetworkV6 &&
+                    remoteEndPoint.AddressFamily == AddressFamily.InterNetwork)
+                {
+                    remoteEndPoint = new IPEndPoint(remoteEndPoint.Address.MapToIPv6(), remoteEndPoint.Port);
+                }
+
                 sendSize = Socket.SendTo(buffer[..size], ConvertBsdSocketFlags(flags), remoteEndPoint);
 
                 return LinuxError.SUCCESS;
@@ -491,6 +534,43 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Impl
             return true;
         }
 
+        private static bool CanSendMMsgHdr(BsdMMsgHdr message)
+        {
+            foreach (BsdMsgHdr msgHeader in message.Messages)
+            {
+                if (msgHeader.Control is { Length: > 0 })
+                {
+                    return false;
+                }
+            }
+
+            return true;
+        }
+
+        private static byte[] GetMessageBuffer(BsdMsgHdr message)
+        {
+            int length = 0;
+
+            foreach (byte[] iov in message.Iov)
+            {
+                length += iov?.Length ?? 0;
+            }
+
+            byte[] buffer = new byte[length];
+            int offset = 0;
+
+            foreach (byte[] iov in message.Iov)
+            {
+                if (iov != null)
+                {
+                    iov.CopyTo(buffer, offset);
+                    offset += iov.Length;
+                }
+            }
+
+            return buffer;
+        }
+
         private static ArraySegment<byte>[] ConvertMessagesToBuffer(BsdMMsgHdr message)
         {
             int segmentCount = 0;
@@ -613,7 +693,7 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Impl
                 return LinuxError.SUCCESS;
             }
 
-            if (!CanSupportMMsgHdr(message))
+            if (!CanSendMMsgHdr(message))
             {
                 Logger.Warning?.Print(LogClass.ServiceBsd, "Unsupported BsdMMsgHdr");
 
@@ -627,14 +707,35 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Impl
 
             try
             {
-                int sendSize = (Socket as DefaultSocket).BaseSocket.Send(ConvertMessagesToBuffer(message), ConvertBsdSocketFlags(flags), out SocketError socketError);
-
-                if (sendSize > 0)
+                foreach (BsdMsgHdr msgHeader in message.Messages)
                 {
-                    UpdateMessages(out vlen, message, sendSize);
+                    byte[] buffer = GetMessageBuffer(msgHeader);
+                    int sendSize;
+
+                    if (msgHeader.Name == null)
+                    {
+                        sendSize = Socket.Send(buffer, ConvertBsdSocketFlags(flags));
+                    }
+                    else if (msgHeader.Name.Length >= Marshal.SizeOf<BsdSockAddr>())
+                    {
+                        IPEndPoint remoteEndPoint = MemoryMarshal.Read<BsdSockAddr>(msgHeader.Name).ToIPEndPoint();
+                        if (Socket.AddressFamily == AddressFamily.InterNetworkV6 &&
+                            remoteEndPoint.AddressFamily == AddressFamily.InterNetwork)
+                        {
+                            remoteEndPoint = new IPEndPoint(remoteEndPoint.Address.MapToIPv6(), remoteEndPoint.Port);
+                        }
+                        sendSize = Socket.SendTo(buffer, ConvertBsdSocketFlags(flags), remoteEndPoint);
+                    }
+                    else
+                    {
+                        return vlen > 0 ? LinuxError.SUCCESS : LinuxError.EINVAL;
+                    }
+
+                    msgHeader.Length = (uint)sendSize;
+                    vlen++;
                 }
 
-                return WinSockHelper.ConvertError((WsaError)socketError);
+                return LinuxError.SUCCESS;
             }
             catch (SocketException exception)
             {
@@ -643,7 +744,7 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Impl
                     Logger.Warning?.Print(LogClass.ServiceBsd, $"Socket Exception: {exception}");
                 }
 
-                return WinSockHelper.ConvertError((WsaError)exception.ErrorCode);
+                return vlen > 0 ? LinuxError.SUCCESS : WinSockHelper.ConvertError((WsaError)exception.ErrorCode);
             }
         }
     }
diff --git a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Types/BsdIoctl.cs b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Types/BsdIoctl.cs
index d38bd02b8..27b8c2301 100644
--- a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Types/BsdIoctl.cs
+++ b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Types/BsdIoctl.cs
@@ -3,5 +3,6 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Types
     enum BsdIoctl
     {
         AtMark = 0x40047307,
+        FionRead = 0x4004667f,
     }
 }
diff --git a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Types/BsdSockAddr.cs b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Types/BsdSockAddr.cs
index 0dc76fc7e..98ea38ceb 100644
--- a/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Types/BsdSockAddr.cs
+++ b/src/Ryujinx.HLE/HOS/Services/Sockets/Bsd/Types/BsdSockAddr.cs
@@ -24,14 +24,29 @@ namespace Ryujinx.HLE.HOS.Services.Sockets.Bsd.Types
 
         public static BsdSockAddr FromIPEndPoint(IPEndPoint endpoint)
         {
+            IPAddress address = endpoint.Address;
+            if (address.IsIPv4MappedToIPv6)
+            {
+                address = address.MapToIPv4();
+            }
+            else if (address.Equals(IPAddress.IPv6Any))
+            {
+                address = IPAddress.Any;
+            }
+
+            if (address.AddressFamily != System.Net.Sockets.AddressFamily.InterNetwork)
+            {
+                throw new ArgumentException("Only IPv4 socket addresses are supported.", nameof(endpoint));
+            }
+
             BsdSockAddr result = new()
             {
                 Length = 0,
-                Family = (byte)endpoint.AddressFamily,
+                Family = (byte)address.AddressFamily,
                 Port = (ushort)IPAddress.HostToNetworkOrder((short)endpoint.Port),
             };
 
-            endpoint.Address.GetAddressBytes().AsSpan().CopyTo(result.Address.AsSpan());
+            address.GetAddressBytes().AsSpan().CopyTo(result.Address.AsSpan());
 
             return result;
         }
