diff --git a/providers/implementations/rands/seed_src.c b/providers/implementations/rands/seed_src.c
index 7a4b780..7e2eb4f 100644
--- a/providers/implementations/rands/seed_src.c
+++ b/providers/implementations/rands/seed_src.c
@@ -22,6 +22,10 @@
 #include "crypto/rand.h"
 #include "crypto/rand_pool.h"
 
+#ifdef __SWITCH__
+# include <switch.h>
+#endif
+
 static OSSL_FUNC_rand_newctx_fn seed_src_new;
 static OSSL_FUNC_rand_freectx_fn seed_src_free;
 static OSSL_FUNC_rand_instantiate_fn seed_src_instantiate;
@@ -94,8 +98,10 @@ static int seed_src_generate(void *vseed, unsigned char *out, size_t outlen,
                              ossl_unused size_t adin_len)
 {
     PROV_SEED_SRC *s = (PROV_SEED_SRC *)vseed;
+#ifndef __SWITCH__
     size_t entropy_available;
     RAND_POOL *pool;
+#endif
 
     if (s->state != EVP_RAND_STATE_READY) {
         ERR_raise(ERR_LIB_PROV,
@@ -104,6 +110,11 @@ static int seed_src_generate(void *vseed, unsigned char *out, size_t outlen,
         return 0;
     }
 
+#ifdef __SWITCH__
+    /* libnx supplies the console CSPRNG; Switch has no Unix entropy device. */
+    randomGet(out, outlen);
+    return 1;
+#else
     pool = ossl_rand_pool_new(strength, 1, outlen, outlen);
     if (pool == NULL) {
         ERR_raise(ERR_LIB_PROV, ERR_R_MALLOC_FAILURE);
@@ -118,6 +129,7 @@ static int seed_src_generate(void *vseed, unsigned char *out, size_t outlen,
 
     ossl_rand_pool_free(pool);
     return entropy_available > 0;
+#endif
 }
 
 static int seed_src_reseed(void *vseed,
